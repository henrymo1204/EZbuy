

*****************************
*   Use of EZbuy code   *
*****************************
Note: make sure you installed the dependencies before continuing.
Follow How_to_enable_JWT.txt for dependency installation. Pip3 is assumed to be installed.

For first time setup:
- install axios (npm i axios)

To run the backend:
1. In Backend/ folder, run
   > sh init.sh
   to initialize the database
2. In Backend/ folder, run
   > sh run.sh
   to start the backend services